@inherits LayoutComponentBase
@using ProsimCompanion.Web.Theming

@* The pop-out Flight Monitor's bare layout (owner request 2026-09-23): no sidebar, no
   header, no footer — the board owns the whole window. Theme tokens still apply so the
   board follows the airline theme like every other page. *@
<ThemeStyle />

<div class="monitor-root">
    @Body
</div>
