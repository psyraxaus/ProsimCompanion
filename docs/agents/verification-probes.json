{
  "version": 1,
  "description": "Machine-checkable verification probes: one entry per GitHub issue whose resolution (or regression) is observable from flight telemetry. Evaluated by an agent after (or during) a flight against the session event log and the CMTrace app log — see docs/agents/flight-verification.md for the workflow, sources and reporting rules. Probes are evidence-gatherers: a passing probe means 'observed working this flight', never 'auto-close'.",
  "sources": {
    "session": "JSONL session event log (sessions/session-*.jsonl): one JSON object per line with timestamp, type, payload.",
    "log": "CMTrace application log (logs/ProsimCompanion-*.log): component, level (type 1=info 2=warn 3=error), message.",
    "wire": "GSX wire trace (logs/ProsimCompanion-wire-*.log): only consulted when a probe names it; often disabled or huge."
  },
  "kinds": {
    "assert-absent": "The failure signature must NOT appear. Pass = absent all session; fail = present (quote the evidence).",
    "assert-present": "The fixed/feature behaviour must appear WHEN its trigger occurred. Pass = trigger+behaviour seen; not-exercised = trigger never occurred (report as untested, not passed); fail = trigger seen without the behaviour.",
    "watch": "Health signal with no fixed pass/fail: count and report occurrences with context.",
    "feature-pending": "Issue is an unimplemented feature; probe documents what to assert once it ships. Skip during evaluation, but report accidental sightings.",
    "requires-human": "Not observable from telemetry (web UI, audio quality, installer). Listed so the flight report can remind the pilot what to eyeball."
  },
  "probes": [
    {
      "id": "phase-startup-misclassification",
      "issue": 59,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session", "log"],
      "checks": [
        "No 'phase-changed' event into InitialClimb/Climb/Cruise/Descent/Approach whose snapshot has iasKt < 30 AND groundSpeedKt < 30 (with #93 shipped, also onGround=true or powered=false — physically impossible airborne states).",
        "No log line 'GSX \"fob restore\": ... -> ... (saved value)' before the session has a genuine airborne phase commit (ias > 80 at the transition). The refusal line ('refused — aircraft has not been airborne') is the CORRECT behaviour.",
        "No 'aircraft-state-check' skipped with reason 'already airborne this session' in a session that never actually flew.",
        "assert-present when ProSim connects after the app started: a 'Flight phase classification suspended ... plausible: False' log line during connection warm-up (the plausibility gate doing its job), followed by 'enabled ... plausible: True' once real data arrives.",
        "Any 'airborne-this-session latch withheld' log line is the hardened latch refusing weak evidence — expected on artifacts, report it with its snapshot fields."
      ],
      "notes": "Fixed in three layers (2026-08-22): physical-plausibility gate (airborne at <30 kt IAS AND GS = hold Unknown), airborne latch requires convincing evidence (ias>=80 or RA>=200 at commit), Unknown->airborne commits carry a 5 s first-classification debounce. The definitive live test remains: start the app, then start ProSim, and confirm no bogus phase, no FOB restore, and the state check still runs."
    },
    {
      "id": "gsx-services-without-flight-plan",
      "issue": 60,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "If a 'gsx-decision' with reason containing 'waiting for a flight plan' exists, no 'gsx-service' {service: Refueling, event: Active} may occur before a later decision shows the plan was detected (simbriefImported=True or fmsOrigin non-empty in the 'flight plan detection' decision)."
      ],
      "notes": "The OFP gate only holds the automation path; GSX-side requests bypass it. Fail = fuel/boarding ran planless."
    },
    {
      "id": "pax-target-thrash",
      "issue": 64,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "Between 'gsx-service' {Boarding, Requested} and {Boarding, Completed}, at most 2 distinct passenger counts across 'gsx-decision' events with action 'pax target' (reason contains 'armed GSX with N passengers')."
      ],
      "notes": "2026-08-16 flight thrashed 89→91→88→85→91→89 from SimBrief pax randomization re-imports during boarding."
    },
    {
      "id": "gate-name-matching",
      "issue": 75,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["log"],
      "checks": [
        "When the anchor runs: a 'gate anchor: anchored GSX to 'X' (code)' decision — and REPORT WHICH ladder rung X was (display name, full facility|gate key, bglName, or bare designator): which identity gate.select matches on is the open empirical question this probe answers.",
        "assert-absent: 'gate.select failed for every identity [...]' — the whole ladder refused means a genuinely unknown-to-GSX stand (cross-check the #44 probe).",
        "Arrival selects at decorated stands ('Stand 547 with Safedock©') accept the unambiguous designator match instead of refusing."
      ],
      "notes": "Fixed 2026-08-22: the anchor walks an identity ladder (not_found advances, any other code stops) and nearest-matching compares the leading designator so decorations never block. The first flight that logs a ladder winner settles the gate.select contract — record it in docs/integrations/gsx-remote-api.md."
    },
    {
      "id": "parking-position-unknown",
      "issue": 44,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session", "log"],
      "checks": [
        "When GSX does not recognize the parking (spawn at an unknown stand): a ground-prep hold with reason 'GSX has not identified the parking — select the stand in the GSX menu, or reposition' (visible on the Flight Status card), AND an 'fo.parking-conflict-advisory' spoken event, once per conflict — including when the only GSX surface is the 'Select Position at ...' menu (the 2026-08-23 gap: only the parking-CHANGE menu was wired, so the hold stayed a log line).",
        "assert-absent: repeated app restarts around a parking-change menu with session gate 'unknown' and no pilot-facing guidance (the 2026-08-17/22 signature).",
        "A 'gsx-question' decision 'position-select menu ... left for the user' when the Select Position menu appears (no more anonymous 'no question handler')."
      ],
      "notes": "Re-fixed 2026-08-23: the unknown-parking HOLD itself now publishes the parking conflict (advisory + status row), so no particular menu shape is required; the Select Position menu is a named catalogue decision. The app still never clicks GSX's parking menus — it advises."
    },
    {
      "id": "ground-ops-double-fire",
      "issue": 90,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No duplicated 'gsx-service' (same service+event pair) within 5 seconds.",
        "No duplicated 'ground.calling'/'ground.upcall' for the same call id within one departure/arrival cycle (a second GPU upcall after a genuine reconnect is legitimate — check the mirror context).",
        "No repeated deice completion events for one deice pass."
      ],
      "notes": "Mirror-vanish reset and deice id matching can re-arm one-shot events within a cycle."
    },
    {
      "id": "failure-detection-truthful",
      "issue": 103,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session", "log"],
      "checks": [
        "No detected->cleared->detected cycle for one failure id within 5 minutes (the 2026-08-23 signature: 4 cycles of elec-gen-1-fault in 6 min, tracking the ECP page button).",
        "No 'failure.cleared' while the fault demonstrably persisted (when instructor-injected: the pilot knows; from telemetry: a re-detect of the same id within minutes is the tell).",
        "assert-present when a failure occurs: 'failure.detected' fires within the definition's debounce WITHOUT any ECP page-button interaction.",
        "watch: count 'trigger is firing but corroborate ... reads low — suppressed' log lines (new visibility) — each is a detection a corroborate blocked; also report 'E/WD text channel is live' vs 'returned no content' (which of the two trigger channels this aircraft actually provides).",
        "watch: any abnormal.line outcome 'fault-remains'/'unable' — the new pilot replies in use."
      ],
      "notes": "Fixed: I_ECAM_* corroborates removed from all shipped definitions (page buttons only light on manual selection); a latched fault clears ONLY via its authored clearedWhen (signal absence is the no-clearedWhen fallback); corroborate suppression logged once per episode; E/WD text channel re-checked after connect; global 'fault remains'/'no change' and 'unable' replies added to every dialogue line."
    },
    {
      "id": "false-ecam-triggers",
      "issue": 58,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No abnormal/ECAM dialogue or master-warning speech for GEN 1/GEN 2 FAULT while no engine has been started this session (pre-start state is normal, not a fault).",
        "No APU FIRE dialogue from a transient shorter than ~2 s (fire signal appears and clears between adjacent events)."
      ],
      "notes": "Verified live 2026-08-23 (1 h powered, engines off, zero false ECAM) — the engine-running AND-condition carries this alone; the I_ECAM corroborate that over-corrected was removed by #103."
    },
    {
      "id": "ecam-dialogue-ignores-cleared",
      "issue": 57,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No abnormal-dialogue prompt (repeat of the same demand) spoken more than 60 s after the underlying failure cleared.",
        "Queued dialogues must not starve: any enqueued dialogue eventually speaks or is withdrawn."
      ],
      "notes": "2026-08-15: FO demanded a gen reset 13 minutes after failure.cleared."
    },
    {
      "id": "ecam-dialogue-inescapable",
      "issue": 56,
      "state": "closed-regression-watch",
      "kind": "watch",
      "sources": ["session"],
      "checks": [
        "Report any abnormal dialogue active > 10 minutes, any interval where the same prompt was spoken 3+ times, and any recognized-but-rejected utterances while a dialogue was awaiting an answer (candidate cancel phrases the pilot tried)."
      ],
      "notes": "Full fix needs a cancel command (feature); the watch quantifies how often the trap bites and captures what the pilot said trying to escape."
    },
    {
      "id": "llm-intent-auth",
      "issue": 66,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["log"],
      "checks": [
        "No '401'/'Unauthorized' from the LLM intent client. If the LLM tier is enabled and any free-form utterance fell through to a clarifier, at least one successful LLM intent call should exist."
      ],
      "notes": "Dead-key scenario: every utterance silently degraded to 'Say again — which field?' for a whole flight."
    },
    {
      "id": "metar-gateway",
      "issue": 62,
      "state": "open",
      "kind": "watch",
      "sources": ["log"],
      "checks": [
        "Count 'Gateway \"metar ...\" attempt N/3 failed' lines per airport. Zero failures + a successful METAR consumption = evidence toward closing; persistent 500s = still broken (confirmed 2026-08-17)."
      ],
      "notes": "Fix will add fallback sources; until then this tracks whether the ProSim gateway endpoint recovered."
    },
    {
      "id": "crash-without-trace",
      "issue": 46,
      "state": "open",
      "kind": "watch",
      "sources": ["session", "log"],
      "checks": [
        "Any session JSONL that does not end with a 'session-ended' event = crash/kill signature; report the last events before the cut. Any CMTrace log that stops mid-write. Correlate with USB/audio-device lines just before the end."
      ],
      "notes": "The issue asks for global unhandled-exception logging; until it ships, the absent-session-ended heuristic is the only crash telemetry."
    },
    {
      "id": "climb-approach-gear-down",
      "issue": 48,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'phase-changed' Climb→Approach whose snapshot shows verticalSpeedFpm > +300 (positive climb must outvote a stuck gear lever)."
      ],
      "notes": "Evaluator already guards InitialClimb/TakeoffRoll; this watches the Climb path with the #93 payload fields."
    },
    {
      "id": "checklist-swallowed-by-atc-parser",
      "issue": 47,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'Copied — conditional' (or other ATC-copy reply) spoken in response to an utterance containing 'checklist'."
      ],
      "notes": "'After start/after takeoff checklist' classified as an ATC conditional without any FCU content."
    },
    {
      "id": "voicemeeter-session-leak",
      "issue": 34,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["log"],
      "checks": [
        "No second VoiceMeeter session/login opened without the previous one closing (pair open/close lines; a mapping change mid-flight is the trigger)."
      ],
      "notes": "Exercise by changing an audio mapping mid-session (also the #35 watchdog scenario)."
    },
    {
      "id": "log-hardening-bundle",
      "issue": 76,
      "state": "open",
      "kind": "watch",
      "sources": ["session", "log", "wire"],
      "checks": [
        "Wire log growth: report final size; > 50 MB in one session = the logging-reload storm signature.",
        "Any GSX trigger sent ('trigger sent — waiting for GSX to confirm') with no confirmation within 60 s = dropped request (the #76 GPU case).",
        "Any turnaround LVAR write followed by a SimConnect disconnect within 2 s (write may be lost).",
        "Count GSX retry-noise warning lines."
      ],
      "notes": "Four sub-items from the 2026-08-16 log review; each occurrence is evidence for prioritizing the fix."
    },
    {
      "id": "isa-advisory-phase-blind",
      "issue": 73,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'speech.spoken' containing 'climb performance' while the committed phase is Cruise or later."
      ],
      "notes": "Wording should become phase-aware; the spoken text in the session log is the normalizer's final output, so this is directly checkable."
    },
    {
      "id": "winds-pronunciation",
      "issue": 69,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'speech.spoken' text matching /\\bwinds \\d/ — the plural form must be expanded by the AviationSpeech normalizer like the singular already is."
      ],
      "notes": "Spoken text is logged post-normalizer, so the raw digit pattern surviving = bug reproduced."
    },
    {
      "id": "nato-phonetics",
      "issue": 68,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'speech.spoken' ATIS text matching /information [a-z]\\b/i without the phonetic word (should read 'information Mike'), and no SID/STAR designator spoken with a bare trailing letter."
      ],
      "notes": "Partially checkable from text; final pronunciation quality still needs ears (see requires-human list)."
    },
    {
      "id": "airport-common-names",
      "issue": 70,
      "state": "open",
      "kind": "feature-pending",
      "sources": ["session"],
      "checks": [
        "Once shipped: briefing/logbook 'speech.spoken' texts contain airport names ('Heathrow') where today they contain bare ICAOs; count bare 4-letter ICAO codes in spoken text as the before/after metric."
      ],
      "notes": "DFD airports table already carries airport_name."
    },
    {
      "id": "set-standard-command",
      "issue": 49,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session", "log"],
      "checks": [
        "'set standard' → 'voicecommand.executed', then 'voicecommand.verified' with ok=true (the EFIS2 STD gate read 1), then spoken 'Standard, cross checked, passing flight level <digits>' — never the confirmation without the verified event.",
        "With #109 shipped: a verify failure triggers ONE automatic retry with a 600 ms hold ('voicecommand.verified' carries retried=true) — 'ok=true, retried=true' means the long hold is what ProSim needed (record that as the press-semantics answer); 'ok=false, retried=true' after the retry is a genuine press-semantics failure to investigate (the 2026-08-23 flight went 0-for-3 at the 150 ms hold).",
        "'set qnh' on descent mirrors this with equals=0 and 'QNH ..., cross checked'."
      ],
      "notes": "Shipped 2026-08-22: {flightLevel} token + per-command verify gate. #109 (2026-08-23) added the single-press verify retry at a 600 ms hold. CAVEAT: a user-edited %LOCALAPPDATA% commands.json keeps its old say/no-verify entries (ADR-0007 keep-user-edits) — if the old 'Standard set' phrasing is heard, the user copy needs a manual merge."
    },
    {
      "id": "sterile-phase-reject-suppression",
      "issue": 67,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session", "log"],
      "checks": [
        "No 'reject'-tagged speech (Didn't catch that / Say again / Repeat please) between a phase commit into TakeoffRoll and the first commit out of InitialClimb, nor during LandingRollout — unmatched utterances there produce 'Idle miss absorbed silently' debug lines instead.",
        "Recognized callouts (engine start, flaps) still answer normally outside those phases."
      ],
      "notes": "Suppression shipped 2026-08-22 (the FO chirped 'Say again?' during rotation on that flight); the full FMA-callout recognition remains open on this issue."
    },
    {
      "id": "gear-voice-command",
      "issue": 43,
      "state": "open",
      "kind": "assert-present",
      "sources": ["session"],
      "checks": [
        "'gear up' after liftoff → a 'voicecommand.gear' {position: up} event, FO speaks 'Gear up.', and NO reject speech (the 2026-08-22 signature was two rejected calls seconds after liftoff).",
        "'gear up' on the ground → refused verbally ('Negative — we are on the ground.') with NO gear event.",
        "'gear down' on approach → 'voicecommand.gear' {position: down} + 'Gear down.'"
      ],
      "notes": "Gear shipped 2026-08-22 as the first non-FCU PM action (S_MIP_GEAR, airborne-gated). The issue stays open for the remaining PM duties (radio tuning, FCU actions, user-selectable scope)."
    },
    {
      "id": "loadsheet-sim-time",
      "issue": 95,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session", "log"],
      "checks": [
        "The loadsheet footer time (HH:mmZ) matches the sim zulu clock captured in the same session (within a few minutes), not the wall clock — decisive whenever the simulated time of day differs from real.",
        "STD-offset prelim decisions ('STD HH:mmZ minus N min reached') fire against the simulated clock."
      ],
      "notes": "Fixed 2026-08-22 via the Core ISimClock seam (zulu time-of-day + sim date, 10 s freshness, real-UTC fallback). With no sim running, loadsheets correctly carry real UTC."
    },
    {
      "id": "api-auth-ergonomics",
      "issue": 96,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": [],
      "checks": [
        "From another machine: a telemetry request with ONLY 'Authorization: Bearer <token>' succeeds (no cookie dance), and '?token=...&tailKb=N' onboarding preserves tailKb through the redirect (response size tracks N)."
      ],
      "notes": "Fixed 2026-08-22 in LanTokenMiddleware. Verified by the evaluating agent itself on its next telemetry pull — this probe is self-exercising."
    },
    {
      "id": "dead-circuit-auto-reload",
      "issue": 97,
      "state": "closed-regression-watch",
      "kind": "requires-human",
      "sources": [],
      "checks": [
        "With the EFB open in a browser, restart the app: the page must reload itself within ~10 s (boot-id watchdog polling /api/app/boot) instead of sitting dead. GET /api/app/boot returns a stable id per process."
      ],
      "notes": "A dead tab leaves no server-side events by definition — human-eyeball only, but it takes 15 seconds to test."
    },
    {
      "id": "voice-ground-ops-wp1-gating",
      "issue": 50,
      "state": "open",
      "kind": "assert-present",
      "sources": ["session", "log"],
      "checks": [
        "When the Voice activation mode is configured: departure services must not auto-start without the voice trigger — 'gsx-decision' shows the gate holding until the pilot's call; with automation mode, auto-start decisions appear as before."
      ],
      "notes": "Trigger-dependent: only evaluable in sessions where voice gating was enabled."
    },
    {
      "id": "voice-ground-ops-wp2-upcalls",
      "issue": 51,
      "state": "open",
      "kind": "assert-present",
      "sources": ["session"],
      "checks": [
        "Ground-crew upcalls fire on their triggers: 'ground.upcall.gpu' after GPU connect, 'ground.upcall.refuel' after Refueling Completed, 'ground.upcall.catering' after Catering Completed; crew hail dialogues respond on INT."
      ],
      "notes": "Upcalls verified live 2026-08-17 (GPU/refuel/catering all spoke). Hail dialogues still unverified — exercise one 'cockpit to ground' hail."
    },
    {
      "id": "accent-localization",
      "issue": 53,
      "state": "open",
      "kind": "assert-present",
      "sources": ["log"],
      "checks": [
        "At a non-home-country airport, the ground-crew voice resolution log shows a locale derived from the airport ICAO prefix."
      ],
      "notes": "WP4; needs a flight from/to an airport whose prefix maps to a non-default accent."
    },
    {
      "id": "intrad-service-skip",
      "issue": 91,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session", "log"],
      "checks": [
        "When INT/RAD is pressed during a running departure sequence: 'gsx-decision' {action: 'force next service', reason: 'requested by INT/RAD'} appears and the next service triggers.",
        "assert-absent: any 'Subscriber handler ... threw System.InvalidCastException' for S_ASP_INTRAD/S_ASP2_INTRAD."
      ],
      "notes": "Fixed 7b8db7a after being silently dead (sender cast). Not exercised = report untested."
    },
    {
      "id": "aircraft-state-recheck",
      "issue": 92,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session"],
      "checks": [
        "Voice 'check the aircraft state' → a fresh 'aircraft-state-check' event AND an 'fo.aircraft-state-advisory' speaking the verdict (Pass wording: 'the aircraft is in the expected cold and dark state').",
        "After a Mismatch verdict, making the aircraft cold and dark (phase-changed → ColdAndDark) → automatic re-check with a spoken all-clear.",
        "If asked while the check cannot run: the FO answers 'Standing by' rather than silence."
      ],
      "notes": "Shipped fd4c47d, never verified live."
    },
    {
      "id": "phase-commit-evidence",
      "issue": 93,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session"],
      "checks": [
        "Every 'phase-changed' snapshot payload carries the full field set (onGround, altitudeFt, radioAltitudeFt, verticalSpeedFpm, powered, enginesRunning, engineStarting, pushback, parkBrake, gearDown, takeoffThrust; with #100 shipped, also beacon, apuRunning, rawPushback; with #105 shipped, also fmsCruiseAltFt) — a payload with only iasKt/groundSpeedKt means an old build is running."
      ],
      "notes": "Doubles as a build-version canary for every other probe that reads these fields."
    },
    {
      "id": "state-check-runs-each-session",
      "issue": 63,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session"],
      "checks": [
        "Each session with a sim flight has exactly one initial 'aircraft-state-check' event (Pass/Mismatch/Skipped with an honest reason); a Mismatch produces the FO advisory when announce is on."
      ],
      "notes": "Verified live 2026-08-17."
    },
    {
      "id": "listening-at-startup",
      "issue": 61,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["log"],
      "checks": [
        "'Recognition \"listening\"' within ~5 s of app start, without a PTT toggle in between."
      ],
      "notes": "Verified live 2026-08-17 (all three sessions)."
    },
    {
      "id": "startup-resync",
      "issue": 30,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["session", "log"],
      "checks": [
        "On an app restart mid-turnaround: resync assessment completes, lifecycle latches seed from tracking LVARs/dataref evidence, and the sequencer holds until assessed (no duplicate service calls after restart).",
        "No 'tracking LVARs claim departure progress the ProSim datarefs contradict' decision within ~15 s of app start (the 2026-08-23 false-stale signature: judged before the plan refs pushed, wiped valid LVARs, re-ran Refueling+Catering and re-pumped FOB mid-boarding).",
        "No 'gsx-service' Requested for a service whose prior session recorded Completed in the same departure cycle."
      ],
      "notes": "Regressed 2026-08-23, re-fixed: the staleness judgement now waits for the plan refs' first push (bounded by the 90 s window) and unknown facts (null) can neither prove nor condemn. Exercised only by restarting the app mid-cycle."
    },
    {
      "id": "sdk-wedge-watchdog",
      "issue": 35,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["log"],
      "checks": [
        "No SDK-dispatcher watchdog trips and no wedged-worker recoveries when audio mappings change mid-flight."
      ],
      "notes": "Deadlock fixed with worker+dispatcher+watchdog; a watchdog LINE FIRING means the underlying wedge returned."
    },
    {
      "id": "ground-prep-idle-stage",
      "issue": 98,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "After the last 'phase-changed' event leaving the ground-prep window (current not Preflight/ColdAndDark), every subsequent 'gsx-ground-prep-stage' event carries stage 'Idle' or 'Complete' until the phase returns to the window — any 'Reposition'/'Settling'/'AnchorGate' stage recorded in flight is a fail.",
        "On a mid-flight app restart: no 'gsx-ground-prep-stage' event with an active stage name before landing."
      ],
      "notes": "Fix: GsxPrepStage.Idle resting state; resets land on Idle, hold-release publishes 'standing by'. The pill itself ('Idle', neutral tone) is on the requires-human list."
    },
    {
      "id": "approach-phase-flapping",
      "issue": 99,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'phase-changed' Approach->Climb whose snapshot has verticalSpeedFpm < 800 (the go-around gate).",
        "No 'phase-changed' into Cruise with radioAltitudeFt < 5000 in arrival context (the 14:09:14 'Cruise at 3,000 ft' signature).",
        "No 'callout.fired' positiveClimb, no 'flapsNotRetracted' speech, and no ISA-deviation advisory between the first Approach commit and touchdown, unless a genuine go-around (sustained VS >= 800 fpm) occurred.",
        "assert-present on a deliberate go-around: Approach->Climb still commits within ~5-10 s of sustained TOGA climb."
      ],
      "notes": "Two layers (2026-08-22): evaluator keeps Approach below +800 fpm; engine adds a 5 s Approach->Climb settle. Reproduced verbatim at LGAV 14:09:03-14:12:38."
    },
    {
      "id": "pushback-flag-semantics",
      "issue": 100,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'phase-changed' TaxiOut->PushbackAndStart whose snapshot has engineStarting=false (a stray pushback reading must never walk a taxi back).",
        "assert-present during a normal GSX pushback with beacon on: a Preflight->PushbackAndStart commit BEFORE engine start (the beacon+APU window)."
      ],
      "notes": "Fix: beacon is a necessary gate (Prosim2FO parity), beacon+APU covers the real push window, and only at-gate phases may enter on pushback evidence. The rawPushback watch item was answered on 2026-08-23 — see probe pushback-enum-mapping (#104). ApuRunning has a benign false fallback — if the APU ref never arrives, entry degrades to engine-start-only (report it)."
    },
    {
      "id": "pushback-enum-mapping",
      "issue": 104,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'phase-changed' snapshot with pushback=true while airborne or during TaxiOut/TakeoffRoll (the inverted-mapping latch signature: pushback=true for the whole 2026-08-23 flight).",
        "assert-present on a normal departure: a PushbackAndStart->TaxiOut commit exists between the push and TakeoffRoll (the stuck-phase signature was 14 min of taxi in PushbackAndStart, rescued only by TakeoffRoll).",
        "watch: report any rawPushback value other than 3 (idle) or 0 (push in progress) with its context — 1/2 are presumed direction variants, unobserved so far."
      ],
      "notes": "Settled enum (2026-08-23 instrumented flight): groundservice.pushback 3 = idle, 0 = push session in progress; mapping inverted accordingly, descriptor fallback = 3. Documented in docs/integrations/prosim.md."
    },
    {
      "id": "cruise-entry-gate",
      "issue": 105,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'phase-changed' into Cruise whose snapshot has fmsCruiseAltFt >= 1000 AND altitudeFt < fmsCruiseAltFt - 2000 (level-offs below the planned cruise level must hold Climb/Descent).",
        "No 'phase-changed' into Cruise with fmsCruiseAltFt < 1000 AND altitudeFt < 10000 (the no-FMS floor).",
        "No more than 2 Climb<->Cruise transitions per climb and 2 Descent<->Cruise per descent (the 2026-08-23 flight had 6 and 3).",
        "No 'flow.advisory' isaDeviation below 10,000 ft.",
        "assert-present: reaching the FMS cruise level and levelling still commits Cruise within ~30 s."
      ],
      "notes": "Fix: evaluator holds the current phase on level flight unless the altitude is cruise-plausible (near aircraft.fms.cruiseAlt, else >= 10,000 ft); established Cruise is exempt (entry-only gate); engine adds a 10 s Cruise->Climb settle. The phase-changed payload carries fmsCruiseAltFt for this check."
    },
    {
      "id": "takeoff-thrust-airborne",
      "issue": 101,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No 'phase-changed' snapshot with onGround=false AND takeoffThrust=true (the flag now means thrust set on the ground)."
      ],
      "notes": "Was an N1-only heuristic: true for the whole climb/cruise on the 2026-08-22 flight, and a latent reverse-thrust->TakeoffRoll trap."
    },
    {
      "id": "good-engine-start-confirm",
      "issue": 106,
      "state": "closed-regression-watch",
      "kind": "assert-present",
      "sources": ["log"],
      "checks": [
        "During a pushback with engine start: after 'vehicle state 12 (awaiting good-engine-start confirmation)', a 'confirmed good engine start — tug clear to disconnect' decision appears without any pilot menu interaction.",
        "assert-absent: 'GSX menu '\"Interrupt pushback?\"' has no question handler' (the 2026-08-23 signature — the pilot had to answer by hand).",
        "A 'good-engine-start confirm failed ... leaving the menu to the pilot' line is the honest give-up path — report it as a menu-shape finding, not a regression."
      ],
      "notes": "Auto-confirm gates (Prosim2GSX parity): park brake set, an engine stably running (any — single-engine taxi is real), none mid-start; entry pattern '^confirm good engine' on the Interrupt-pushback menu, retry-capped, fails safe to the pilot."
    },
    {
      "id": "checklist-answer-acceptance",
      "issue": 107,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No more than ONE reject-tagged speech while a single checklist item is open (further misses are absorbed with a debug log).",
        "No reject-tagged speech in Approach below 1,000 ft RA (short-final absorb).",
        "assert-present when exercised: an utterance containing 'as required' or 'checked' completes a non-number checklist item (the verify backstop still cross-checks the switch)."
      ],
      "notes": "2026-08-23 lineup: 'As required' rejected 3x on the T CAS item with the spoken nag escalation, plus a 'Repeat please.' between the 1,000 and 500 ft calls on final."
    },
    {
      "id": "callout-duplication",
      "issue": 108,
      "state": "closed-regression-watch",
      "kind": "assert-absent",
      "sources": ["session"],
      "checks": [
        "No identical 'callout.fired' id twice within 10 s (2026-08-23: spoilers 2x in 100 ms at touchdown, oneThousandToGo 8x in 1.5 s while the FCU knob was wound).",
        "oneThousandToGo fires at most once per settled FCU target and never inside its ~15 s cooldown."
      ],
      "notes": "Fixes: rollout latches re-arm on Approach entry (the LandingRollout commit raced the 100 ms sample tick), oneThousandToGo requires a knob-settled target plus a cooldown."
    },
    {
      "id": "fo-flight-live-gate",
      "issue": 114,
      "state": "open",
      "kind": "assert-absent",
      "sources": ["session", "log"],
      "checks": [
        "No 'failure.detected', 'abnormal.*', 'callout.*', cabin, ground-upcall or company-channel speech event before the first 'Flight live true' log line of the session (FlightStateEngine). Any FO speech with the app up and MSFS on the menu / not running is the 2026-08-29 signature.",
        "assert-present after session exit (menu/world map): 'Flight live false' log line, then — if a fault was latched — 'abnormal trigger latches reset'; a running ECAM dialogue ends with 'abnormal.ended' reason 'session-ended' and NO 'ECAM cancelled' / 'has cleared' announcement.",
        "assert-present on the next session start: a still-present fault is re-detected ('failure.detected') after its normal debounce — the latch did not survive the session boundary.",
        "watch: the 'Flight live' true/false edge count per session — more than a handful indicates readiness/plausibility flapping that would mute the FO mid-flight."
      ],
      "notes": "Fixed: IFlightPhaseSource.IsLive (session data-meaningful AND datarefs ready AND plausible) is the single FO arming gate; FailureMonitor, FlowMonitor, StabilizedApproachMonitor, CalloutsEngine, CabinCrewService, GroundCrewUpcallService, CompanyChannelService and MissedApproachRebrief hold on it; an empty abnormal 'phases' list no longer arms in Unknown. PTT/recognition and TTS prewarm are deliberately NOT gated (TTS/STT server configuration must remain testable with no sim)."
    },
    {
      "id": "requires-human-checklist",
      "issue": 0,
      "state": "open",
      "kind": "requires-human",
      "sources": [],
      "checks": [
        "#71 EFB header clock shows sim time when connected (web UI).",
        "#65 landing performance runway select enables after OFP import (web UI).",
        "#74/#55 config parse failures and loaded-config path visible in the web UI.",
        "#54 settings GUI backfill completeness (web UI).",
        "#45 Services card shows ground-prep progress (web UI; the underlying gsx-ground-prep-stage events already flow).",
        "#98 Ground Prep pill reads 'Idle' with the neutral tone in flight and after a mid-flight app restart (web UI).",
        "#68/#69 final TTS pronunciation quality (ears, beyond the text-level probes).",
        "#97 dead-circuit auto-reload: restart the app with the EFB open — the page must reload itself (~10 s).",
        "Parity/features not yet implemented: #42, #72, #3–#12 (plus the unshipped remainders of #43 and #67)."
      ],
      "notes": "Issue 0 = not a real issue number; this entry is the pilot's eyeball list appended to every flight report."
    }
  ]
}
